# Quick Test Configuration
# Minimal test run for framework validation

name: test
type: DummyEnv

# Minimal test environment
params:
  observation_dim: 4
  action_dim: 2
  episode_length: 10
  reward_range: [-1.0, 1.0]
