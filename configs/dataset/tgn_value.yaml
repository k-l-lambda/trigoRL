# TGN Value Dataset Configuration
# For training dual-head networks (policy + value)

type: TGNValueDataset
data_dir: ${paths.root}/third_party/trigo/trigo-web/tools/output/selfplay
max_length: 8192
min_length: 10
max_file_size: 10000
parse_value: true
tokenizer_config: {}
